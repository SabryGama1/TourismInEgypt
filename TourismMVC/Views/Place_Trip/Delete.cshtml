@model Place_Trip
@{
    ViewData["Title"] = "Delete";
}
<div class="card text-dark bg-light mb-3" style="width:100% ; height: 100%">
    <div class="card-header" style="font-size:x-large ; font-family:Arial ; font-style:italic ; font-weight:bold ; color :darkblue">Delete Card</div>
    <div>

        <partial name="PartialViews/DetailsDeleteView" />


        <div class="form-group mt-4">
            <form method="post" asp-action="Delete">
                <input type="submit" value="Save" class="btn btn-primary" style="margin-left : 20px" />
                <a asp-action="Index" class="btn btn-light">Back to List</a>

            </form>
        </div>

    </div>
    </div>
